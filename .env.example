# ============================================
# SECURITY NOTICE
# ============================================
# DO NOT use these example values in production!
# Generate strong passwords (32+ characters) using:
#   pwsh: -join ((33..126) | Get-Random -Count 32 | % {[char]$_})
#   bash: openssl rand -base64 32
# 
# REQUIRED: You MUST set these values before running docker-compose up
# Missing required values will cause startup failures.
# ============================================

# Database Configuration (REQUIRED)
DB_ROOT_PASSWORD=
DB_NAME=wordpress
DB_USER=wpuser
DB_PASSWORD=

# WordPress Configuration (REQUIRED)
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=
WP_DEBUG=false

# AI Service API Keys (REQUIRED for AI features)
# Get from: https://platform.openai.com/api-keys
#           https://console.anthropic.com/
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Environment
ENVIRONMENT=development
NODE_ENV=development
LOG_LEVEL=info

# URLs
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WP_URL=http://localhost:8080

# Stripe (for monetization - OPTIONAL)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# JWT Secret (REQUIRED - minimum 32 characters)
# Generate: pwsh: -join ((33..126) | Get-Random -Count 32 | % {[char]$_})
JWT_SECRET=
