# ============================================
# VAULT APPROLE CREDENTIALS
# ============================================
# This file contains AppRole credentials for authenticating to Vault.
# 
# Setup Instructions:
# 1. Copy this file to .env.vault (already in .gitignore)
# 2. Replace the placeholder values with your actual role_id and secret_id
# 3. Get credentials from Vault workspace using:
#    vault read auth/approle/role/{role-name}/role-id
#    vault write -f auth/approle/role/{role-name}/secret-id
# ============================================

# Vault Server Address
VAULT_ADDR=http://localhost:8200

# AI Service AppRole Credentials
AI_SERVICE_ROLE_ID=your-ai-service-role-id-here
AI_SERVICE_SECRET_ID=your-ai-service-secret-id-here

# Frontend AppRole Credentials
FRONTEND_ROLE_ID=your-frontend-role-id-here
FRONTEND_SECRET_ID=your-frontend-secret-id-here

# WordPress AppRole Credentials
WORDPRESS_ROLE_ID=your-wordpress-role-id-here
WORDPRESS_SECRET_ID=your-wordpress-secret-id-here

# MySQL AppRole Credentials (can reuse WordPress credentials for simplicity)
MYSQL_ROLE_ID=your-mysql-role-id-here
MYSQL_SECRET_ID=your-mysql-secret-id-here
